{
    "swagger": "2.0",
    "info": {
      "title": "Sample API",
      "description": "A sample API that demonstrates how to use Flask-SwaggerUI.",
      "version": "1.0"
    },
    "basePath": "/",
    "schemes": [
      "http"
    ],
    "consumes": [
      "application/json"
    ],
    "produces": [
      "application/json"
    ],
    "paths": {
      "/get_data?set=10&limit=20": {
        "get": {
          "summary": "Returns data from table with set and limit parameters. Set parameter defines which id to start getting the data and limit parameter defines how many row to get.",
          "produces": [
            "application/json"
          ],
          "responses": {
            "200": {
              "description": "Successful response."
            }
          }
        }
      }
    }
  }